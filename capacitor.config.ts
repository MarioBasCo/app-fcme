import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.stip.fcme.app',
  appName: 'fcme',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
